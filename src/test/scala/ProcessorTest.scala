/**
 * Created by indix on 12/2/15.
 */
/**
import org.scalatest.FlatSpec
import org.scalatest.Matchers._

class ProcessorTest extends FlatSpec {

  "Processortest" should "test if the processor is able to work with the functions defined to it" in {
    val p = new Processor
    p.commit() should be(0)
    p.respond('a') should be('a')
    p.respond('b') should be('b')
    p.rollback() should be(0)
  }
}
*/